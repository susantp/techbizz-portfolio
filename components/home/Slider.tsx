import React, {FC} from 'react';

const Slider: FC = (): JSX.Element => {
    return (
        <div className={`bg-amber-100`}>
            <div className={`container mx-auto h-64 mt-2`}>Sliders</div>
        </div>
    );
}

export default Slider;